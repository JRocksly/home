describe('OutgoingService', function() {

  beforeEach(module('home'));

  it('should ...', inject(function(OutgoingService) {

	//expect(OutgoingService.doSomething()).toEqual('something');

  }));

});
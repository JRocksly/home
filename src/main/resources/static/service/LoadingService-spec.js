describe('LoadingService', function() {

  beforeEach(module('static'));

  it('should ...', inject(function(LoadingService) {

	//expect(LoadingService.doSomething()).toEqual('something');

  }));

});